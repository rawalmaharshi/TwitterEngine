defmodule Proj4.Application do
  # See https://hexdocs.pm/elixir/Application.html
  # for more information on OTP Applications
  @moduledoc false

  use Application

  def start(_type, _args) do
    # numUsers = String.to_integer(Enum.at(System.argv(),0), 10)
    # numTweets = String.to_integer(Enum.at(System.argv(),1), 10)

    # if numUsers <= 1 do
    #   IO.puts "Please enter the number of users greater than 1"
    #   System.halt(0)
    # end

    # if numTweets < 1 do
    #   IO.puts "Please enter the number of tweets more than 1"
    #   System.halt(0)
    # end   
  end
end